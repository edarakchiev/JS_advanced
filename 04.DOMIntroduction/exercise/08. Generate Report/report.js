function generateReport() {
    let rows = Array.from(document.querySelectorAll("thead th"))

    console.log(rows)
    // for (let i = 0; i < checkboxes.length; i++) {
    //     console.log(checkboxes[i])
    // }
}